{
    "buildCommand": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.144.2/hugo_extended_0.144.2_Linux-64bit.tar.gz | tar -xz && mv hugo /usr/local/bin/ && npm install && cd site && hugo --gc --minify",
    "outputDirectory": "site/public"
}
